import SwiftUI

/// First-run onboarding flow (PRD §5.8 mention, full copy deck in
/// `docs/onboarding-copy.md`). Implementation deferred to M5.
struct OnboardingView: View {
  var body: some View {
    Text("Onboarding")
  }
}
